
import React from 'react';






const BarChart = (props) => {
  // Ruta de la imagen que deseas mostrar
  const imagePath = './Images/bar.jpg'; // Asegúrate de proporcionar la ruta correcta a tu imagen

  return (
    <div>
      
      <img src={imagePath} />
    </div>
  );
}

export default BarChart;